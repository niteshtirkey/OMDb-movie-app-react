import React from "react";
// import { useRouteError } from "react-router-dom";

function Error() {
    // const error = useRouteError();
    // console.log(error);
    return (
        <div style={{ textAlign: "center" }}>Opps! Somthing went wrong...</div>
    );
}

export default Error;
